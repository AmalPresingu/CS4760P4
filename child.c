#include "config.h"

int main(){

    	// wait on some semaphore to start 
        // read the  shared memory and get the pid and time quantum
        // generate random numbe to decide the teermination of tghe proess
        // if termination 
        //reply the oss with the info and random runtime less than tghe quantum
        // break the loop
        // else if not
        // if blocking reply with the info and random runtime less than tghe quantum
        // else reply the oss with the info of usage of full time usage
    return 0;
}
